extends ../layout

block content
    h1 Genre: #{genre.name}

    div.ml-4.mt-4
        h4 Books

        dl.my-1
            each book in genre_books
                dt
                    a(href=book.url) #{book.title}
                dd #{book.summary}
            else
                p This genre has no books.
