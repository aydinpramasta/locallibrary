extends ../layout

block content
    h1 Delete ID: #{bookInstance._id}

    p #[strong Title:]
        a(href=bookInstance.book.url).ml-1 #{bookInstance.book.title}
    p #[strong Imprint:] #{bookInstance.imprint}

    p #[strong Status:]
        if bookInstance.status === 'Available'
            span.text-success.ml-1 #{bookInstance.status}
        else if bookInstance.status === 'Maintenance'
            span.text-danger.ml-1 #{bookInstance.status}
        else
            span.text-warning.ml-1 #{bookInstance.status}

    if bookInstance.status !== 'Available'
        p #[strong Due back:] #{bookInstance.due_back_formatted}

    p Do you really want to delete this Book Instance?

        form(method='POST')
            div.form-group
                input#bookInstanceId.form-control(type='hidden', name='bookInstanceId', required, value=bookInstance._id )

            button.btn.btn-primary(type='submit') Delete
